config BR2_PACKAGE_MIRA_INPUTD
	bool "mira-inputd"
	help
	  HDMI-CEC to uinput bridge, so TV remote keys arrive as ordinary input events.

	  Not yet implemented - placeholder so the Mira menu resolves.
